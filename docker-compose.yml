services:
  geospatial:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app/app
      - shared-data:/data
    environment:
      - DATA_DIR=/data
      - REDIS_URL=redis://redis:6379

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  shared-data:
